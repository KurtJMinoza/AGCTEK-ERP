<?xml version='1.0' encoding='UTF-8'?>
<!DOCTYPE properties SYSTEM 'http://java.sun.com/dtd/properties.dtd'>
<properties>
    <!-- Embedded H2 for local/dev. Replace with PostgreSQL for production if needed. -->
    <entry key='database.driver'>org.h2.Driver</entry>
    <entry key='database.url'>jdbc:h2:./data/database</entry>
    <entry key='database.user'>sa</entry>
    <entry key='database.password'/>

    <!--
      Position forward → AGCTEK Nest ingest (system of record = GpsLog).
      Traccar remains the only GPS protocol decoder (Huabao :5015 for VL502).

      1. Set TRACCAR_INGEST_TOKEN in backend/.env to the same secret used below.
      2. Uncomment forward.* after Nest is reachable from this container.
         From Docker → host Nest: use host.docker.internal (Windows/Mac)
         or the LAN IP of the machine running Nest.
    -->
    <!--
    <entry key='forward.enable'>true</entry>
    <entry key='forward.type'>json</entry>
    <entry key='forward.url'>http://host.docker.internal:3001/scm/tracking/ingest</entry>
    <entry key='forward.header'>X-Traccar-Token: REPLACE_WITH_TRACCAR_INGEST_TOKEN</entry>
    -->

    <!-- Optional: also forward events (ignition, geofence, etc.) to the same URL -->
    <!--
    <entry key='event.forward.type'>json</entry>
    <entry key='event.forward.url'>http://host.docker.internal:3001/scm/tracking/ingest</entry>
    <entry key='event.forward.header'>X-Traccar-Token: REPLACE_WITH_TRACCAR_INGEST_TOKEN</entry>
    -->
</properties>
